{
  "name": "expedition",
  "version": "2.0.0",
  "description": "The Expedition Monorepo",
  "homepage": "http://expeditiongame.com",
  "repository": "https://github.com/ExpeditionRPG/expedition",
  "author": "Fabricate.IO",
  "scripts": {
    "test": "set -e && cd shared && yarn test && cd .. && for D in services/*; do ([ -d $D ] && cd $D && yarn test); done",
    "install": "set -e && cd shared && npm install && cd .. && for D in services/*; do ([ -d $D ] && cd $D && npm install); done"
  },
  "license": "CC-BY-NC-ND-4.0",
  "semistandard": {
    "globals": [
      "after",
      "afterEach",
      "before",
      "beforeEach",
      "describe",
      "it"
    ]
  },
  "engines": {
    "node": ">= 8.1.4"
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.8",
    "node-sass": "^4.9.0",
    "pre-push": "^0.1.1"
  }
}

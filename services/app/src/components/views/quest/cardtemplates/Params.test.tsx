describe('Card template params', () => {
  describe('resolveParams', () => {
    it('extracts node, decision, and combat');
    it('resolves node from storage if node not given');
    it('handles undefined combat state');
    it('handles undefined decision state');
  });
});

describe('CommitID', () => {
  describe('MULTIPLAYER_COMMIT', () => {
    test('updates commitID if commit is greater', () => {
      throw new Error('todo');
    });
    test('keeps commitID if existing ID is greater', () => {
      throw new Error('todo');
    });
  });
});

# Logs
logs
*.log
npm-debug.log*
docker_context

# New file in node 8... removed June 26, 2017 due to outstanding issues with the lock file
# when building on different platforms (aka prod)... can add back once Node resolves
package-lock.json

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release
dist
static
dll/

# Dependency directories
node_modules
jspm_packages

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Secrets (keys, passwords, etc)
config.json

describe('Checkout set state action', () => {
  // Entirely glue code; no testing needed right now.
});

describe('toCheckout set state', () => {
  it('Navigates to checkout if logged in');
  it('Navigates to login if not logged in');
});


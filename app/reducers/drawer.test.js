import expect from 'expect'
import {drawer} from './drawer'

describe('drawer', () => {
  it('sets state on load')

  it('assigns quest list')

  it('auto-closes on load')
  it('auto-closes on save')
  it('auto-closes on delete')
  it('auto-closes on publish')
})
import expect from 'expect'
import {dirty} from './dirty'

describe('dirty', () => {
  it('returns initial state')

  it('sets dirty')

  it('auto-clears on save')

  it('auto-clears on delete')

  it('auto-clears on new')
})
import expect from 'expect'

describe('QuestAppBarContainer', () => {
  it('maps state')

  it('dispatches drawer change with onDrawerToggle')

  it('dispatches user dialog with onUserDialogRequest')
})
describe('Dialogs', () => {
  describe('ExitQuestDialog', () => {
    it('renders if open');

    it('hides if not open');

    it('calls onExitQuest on Exit tap');

    it('closes on Cancel tap');
  });
});

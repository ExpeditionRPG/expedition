import expect from 'expect'

describe('QuestDrawerContainer', () => {
  it('maps state')

  it('dispatches quest action with onMenuSelect')

  it('dispatches drawer close with onDrawerRequestChange')
})
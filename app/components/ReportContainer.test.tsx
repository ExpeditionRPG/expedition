describe('ReportContainer', () => {
  it('prevents submission if no description');
  it('prevents submission on too short description');
  it('Logs in user if user not authenticated and report submitted');
});

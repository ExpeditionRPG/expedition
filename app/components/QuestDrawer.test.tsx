import expect from 'expect'
import * as React from 'react'
import QuestDrawer from './QuestDrawer'

describe('QuestDrawer', () => {
  it('renders if open')

  it('hides if closed')

  it('Shows list of quests')

  it('calls onMenuSelect on New tap')

  it('calls onMenuSelect on Save tap')

  it('calls onMenuSelect on Publish tap')

  it('calls onMenuSelect on Download tap')

  it('calls onMenuSelect on Delete tap')

  it('calls onMenuSelect on listed quest tap')
})